{
  "name": "college-waste-management",
  "version": "1.0.0",
  "description": "MERN Stack Geofencing Waste Management System",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-deps": "cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "mern",
    "geofencing",
    "waste-management",
    "college"
  ],
   "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "axios": "^1.12.2"
  }
}
